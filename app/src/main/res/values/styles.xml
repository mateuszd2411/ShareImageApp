<resources>

    <!-- Light/Day application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="backgroundColor">@color/colorWhite</item>
        <item name="cardBackground">@color/colorCard</item>

        <item name="textColor">@color/colorBlack</item>
        <item name="textHintColor">@color/colorGray</item>

        <item name="tintColor">@color/colorTint</item>
        <item name="buttonColor">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- Night/Dark application theme. -->
    <style name="darkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/colorBg</item>
        <item name="cardBackground">@color/colorCardDark</item>
        <item name="textColor">@color/colorWhite</item>
        <item name="tintColor">@color/colorTintDark</item>
        <item name="buttonColor">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!--    Fade out/in Animation-->
    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>


    </style>

</resources>
